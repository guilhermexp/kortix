[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "corepack enable",
  "corepack prepare bun@latest --activate",
  "bun install --filter='!kortix-browser-extension'"
]

[phases.build]
cmds = ["cd apps/web && bun run build"]

[start]
cmd = "cd apps/web && bun run start"
