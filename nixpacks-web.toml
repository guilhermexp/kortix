[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["corepack enable", "corepack prepare bun@latest --activate", "bun install"]

[phases.build]
cmds = ["cd apps/web && bun run build"]

[start]
cmd = "cd apps/web && bun run start"
