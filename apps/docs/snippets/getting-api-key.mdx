Head to `http://localhost:3000`, faça login com um administrador e abra **Configurações → API Keys**. Gere um token e copie o valor exibido; ele será usado no header `Authorization: Bearer <token>` em todas as requisições contra o backend (`http://localhost:4000`).
