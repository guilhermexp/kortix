[phases.setup]
nixPkgs = ["nodejs_22", "bun", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
    "bun install",
    "cd apps/api && bash install-markitdown.sh"
]

[phases.build]
cmds = ["bun run check-types"]

[start]
cmd = "cd apps/api && bun run start"
